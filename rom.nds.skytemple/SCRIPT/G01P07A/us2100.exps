
def 0 {
    supervision_Station(0);
    if ($DUNGEON_OPEN_LIST[64]) {
    } else {
        supervision_Station(10);
    }
    end;
}

def 1 for_actor(ACTOR_NPC_RURIRI) {
    switch ( sector() ) {
        case 10:
            SetAnimation(45);
            §label_8;
            end;
        default:
            jump @label_8;
    }
}
